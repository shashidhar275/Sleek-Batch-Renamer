const path = require('path');
const replaceThis = 'virat';
const replaceWith = 'Shashidhar';
const preview = false;
const folder = path.join(__dirname,'../');

module.exports = {replaceThis,replaceWith,preview,folder};